import { StyleSheet } from 'react-native';

export const markerIconStyles = StyleSheet.create({
  iconContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 2,
  },
  disabledContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 2,
    opacity: 0.6,
  },
});